services:
  ha-voice-agent:
    build: .
    container_name: ha-voice-agent
    restart: unless-stopped
    ports:
      - "10100:10100"
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml